Con questo documento condivido eventuali dubbi, domande che mi pongo o eventuali idee, in maniera sistematica. 

Possibili aggiunte:

1) interfaccia grafica per il client
2) Metriche del knn
3) Imputer knn
4) Mostrare grafici 
5) Clustering 

dovresti considerare il caso in cui venga salvato un valore nullo in example

for(int i=0;i<e.getSize();i++)
			System.out.println(e.get(i));
			
			
08/07/2022

Adesso fondamentalmente funziona tutto. Dovrai però testare ancora le funzionalità di calcolo. Controlla che 
la predizione sia corretta.

Prossima volta:

1) Crea applicazione client-server
2) Controlla i valori predittivi (Secondo me non calcola bene, vedi bene algo)
3) Cerca di capire una grafica semplice 		


Per poter calcolare correttamente l'errore, devo partire inanzitutto dallo splitting dei dati 
in dati di training e dati di test. Questo l'ho fatto, adesso dovrò fare una serie di cose.
Prima di tutto devo aggiungere il metodo "fit", poichè dovrò addestrare il mio dataset
non sui dati di training ma bensi sui dati di test.

model.fit significa che per ogni riga del dataset devo fare predizione e costruire un dataset
equivalente ma con i valori da me calcolati.
Successivamente dovrò 